{
  "name": "midikit",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/mocha 'test/**/*.js'",
    "doc": "node_modules/.bin/jsdoc src --configure ./jsdoc.json --recurse --destination doc --readme docs-home.md",
    "doc-watch": "watch 'npm run doc' src/ --wait=1"
  },
  "author": "Adam Murray",
  "license": "MIT",
  "optionalDependencies": {
    "midi": "^0.9.5"
  },
  "devDependencies": {
    "jsdoc": "^3.4.0",
    "mocha": "^2.5.3"
  }
}
